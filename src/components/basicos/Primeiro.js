import React from "react";

export default function Primeiro() {
    //se coloca os parentes para poder colocar mais elementos para ser retornado
    const msg = 'Seja Bem vindo'
    return (
        <div>
            <h2>Primeiro Component</h2>
            <p>{msg}</p>
        </div>
    )
}
